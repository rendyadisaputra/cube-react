declare type Repository = {
    name: string;
    owner: string;
};
export declare class PackageFetcher {
    private repo;
    protected readonly tmpFolderPath: string;
    protected repoArchivePath: string;
    constructor(repo: Repository);
    protected init(): void;
    manifestJSON(): Promise<any>;
    protected downloadRepo(): Promise<unknown>;
    downloadPackages(): Promise<{
        packagesPath: string;
    }>;
    cleanup(): void;
}
export {};
//# sourceMappingURL=PackageFetcher.d.ts.map