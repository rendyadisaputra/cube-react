export declare type Package = {
    name: string;
    version: string;
    installsTo: string[] | null;
    receives: Record<string, string>;
};
export declare class DependencyTree {
    private manifest;
    private templatePackages;
    protected rootNode: any;
    protected resolved: any[];
    constructor(manifest: Record<string, unknown>, templatePackages: string[]);
    protected packages(): Package[];
    getRootNode(): any;
    protected packagesInstalledTo(name: any): Record<string, unknown>;
    protected getChildren(pkg: any): any[];
    protected build(node: any): void;
}
//# sourceMappingURL=DependencyTree.d.ts.map