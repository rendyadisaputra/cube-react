import { AuthObject } from './cloud';
export declare class LivePreviewWatcher {
    private watcher;
    private handleQueueTimeout;
    private cubeCloudClient;
    private auth;
    private queue;
    private uploading;
    private lastHash;
    private log;
    setAuth(token: string): AuthObject;
    startWatch(): void;
    stopWatch(message?: string): void;
    getStatus(): Promise<{
        lastHashTarget: string | undefined;
        uploading: Boolean;
        active: boolean;
    }>;
    createTokenWithPayload(payload: Record<string, any>): Promise<any>;
    private handleQueue;
    private deploy;
}
//# sourceMappingURL=live-preview.d.ts.map